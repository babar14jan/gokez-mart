# Set this exact variable in Cloudflare Pages before building the customer app.
# Use the Railway service's public domain, including the API prefix.
VITE_API_URL=https://your-railway-service.up.railway.app/api/v1